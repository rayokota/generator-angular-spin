language: node_js
node_js:
  - '0.10'
  - '0.8'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-angular-spin' ]; then cd .. && eval "mv $currentfolder generator-angular-spin" && cd generator-angular-spin; fi

